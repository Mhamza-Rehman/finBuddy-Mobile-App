<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use Material3 theme as the parent to prevent crashes when using Material3 components -->
    <style name="Theme.FinBuddy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#0F9D58</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>